<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro</title>
</head>
<body>
    

    <script>
        function comprobarDisponibilidad(nombreUsuario, correoElectronico){
            fetch('/usuarios/comprobar-disponibilidad/${nombreUsuario}/${correoElectronico}')
            .then(response => response.text())
            .then(results => {
                if(result === "True"){
                    console.log("El nombre de usuario y correo electrónico ya están disponibles"); 
                } else{
                    console.log(result); 
                } 
            })
            .catch(error => {
                console.error("Error al comprobar disponibilidad", error); 
            }); 
        } 
    </script>
</body>
</html>